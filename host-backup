#!/bin/bash

FNAME=`hostname`_`date +%Y%m%d`

xe pool-dump-database file-name=${FNAME}.pool

xe host-backup name-label=`hostname` file-name=${FNAME}.host

tar czpvf ${FNAME}.conf.tar.gz \
        /root/vm-backup \
        /root/host-backup \
        /root/sms_sms.ru \
	/etc/sysconfig/modules/* \
        /etc/mdadm.conf \
        /etc/ups/* \
        /etc/ssmtp/* \
        /etc/zabbix/* \
        /etc/rc.d/rc.local

xe host-restore file-name=${FNAME}.host

/root/sms_sms.ru ${PHONE} "Host ${FNAME} backup complete."

